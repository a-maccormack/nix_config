# User/Group IDs (run `id` to find yours)
PUID=1000
PGID=1000

# Timezone
TZ=America/Santiago

# Server IP for Jellyfin (update with your server's IP or Tailscale IP)
SERVER_IP=192.168.1.100

# Paths on 4TB HDD
MEDIA_PATH=/srv/media
DOWNLOADS_PATH=/srv/downloads
CONFIG_PATH=/srv/config
